# RP2350 Pico SDK Platform

This directory is reserved for Pico SDK platform adapters that should not leak
into application code.

Current low-level access is still thin and uses the Pico SDK directly from
board and driver modules. Add wrappers here when platform coupling grows.
