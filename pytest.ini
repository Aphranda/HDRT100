[pytest]
testpaths =
    tests/python
    tests/hil
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra
markers =
    unit: host-side unit tests that do not require target hardware
    hil: hardware-in-the-loop tests; require --run-hil and --hil-port
